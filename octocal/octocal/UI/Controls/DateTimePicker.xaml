<UserControl x:Class="octocal.UI.Controls.DateTimePicker"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel>
        <StackPanel Orientation="Horizontal">
            <ComboBox ItemsSource="{Binding DateTime.Days}" SelectedItem="{Binding DateTime.SelectedDay}" />
            <ComboBox ItemsSource="{Binding DateTime.Months}" SelectedItem="{Binding DateTime.SelectedMonth}"
                                  DisplayMemberPath="Value" />
            <ComboBox ItemsSource="{Binding DateTime.Years}" SelectedItem="{Binding DateTime.SelectedYear}" />
        </StackPanel>
        <StackPanel Orientation="Horizontal">
            <ComboBox ItemsSource="{Binding DateTime.Hours}" SelectedItem="{Binding DateTime.SelectedHour}" />
            <ComboBox ItemsSource="{Binding DateTime.Minutes}" SelectedItem="{Binding DateTime.SelectedMinute}" />
        </StackPanel>
    </StackPanel>
</UserControl>
